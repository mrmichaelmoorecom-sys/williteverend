{
  "name": "williteverend",
  "private": true,
  "version": "0.1.0",
  "description": "Will it ever end? \u2014 williteverend.com",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test test/",
    "flip": "wrangler kv key put --binding STATE override YES --remote",
    "unflip": "wrangler kv key delete --binding STATE override --remote"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
